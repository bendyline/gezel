# ds4 (DwarfStar) upstream pin — antirez's specialized MoE inference engine.
#
# Format: same as the other engines (upstream / tag / commit). ds4 cuts
# no release tags (it's developed on `main`), so `tag` is a human-readable
# date label and `commit` is the full 40-char sha the build actually
# checks out. Bump by editing `tag` + `commit` together.
#
# ds4 is beta-quality and moves fast; pin deliberately and re-validate the
# supported DeepSeek/GLM GGUF compatibility (the tested GGUFs and engine are
# co-versioned) when bumping.
#
# License: MIT (https://github.com/antirez/ds4)
upstream=https://github.com/antirez/ds4.git
tag=main-2026-09-16
commit=8db1d1d155cb0400a86a86b9c62d0defb3a6148b
