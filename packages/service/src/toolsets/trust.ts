// Compatibility re-export for existing service-local imports. The predicate
// lives in core so the MCP launch rail cannot drift from the install route.
export { isTrustedConstrainedToolset } from '@bendyline/gezel';
