{
  "planAppend": "### Gezel adaptations\n\nRender font and color proposals as workspace HTML files (e.g. `previews/typography.html`, `previews/palette.html`) so the user can open them in the preview pane — the source skill's `$D` design binary does not exist here. Web research needs the web-search toolset and the user's consent, exactly as the source phases describe."
}
