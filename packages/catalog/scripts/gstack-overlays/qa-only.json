{
  "set": {
    "toolsets": [
      {
        "toolsetId": "@playwright/mcp",
        "optional": false,
        "reason": "drive the page under test — every check in this book runs in a real browser"
      }
    ]
  },
  "planAppend": "### Gezel adaptations\n\nWherever the source text says `$B <command>`, use gezel's browser tools instead: `browser_*` tools for one-shot interactions and `run_playwright_script` for repeatable flows. Screenshots and console logs come from those tools; there is no external browse daemon."
}
