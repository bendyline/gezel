// Browser-facing compatibility facade. The constants live in core so the
// renderer and Electron's pre-renderer failure path cannot drift.
export { GITHUB_REPO_URL, ISSUES_NEW_URL, RELEASES_URL, releaseUrl } from '@bendyline/gezel';
